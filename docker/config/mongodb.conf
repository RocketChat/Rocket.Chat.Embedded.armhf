# /etc/mongodb.conf
# minimal config file (old style)
# Run mongod --help to see a list of options

# This is open to the entire cluster
# bind_ip = 127.0.0.1
bind_ip = 0.0.0.0
quiet = true
dbpath = /var/lib/mongodb
logpath = /var/log/mongodb/mongod.log
logappend = true
storageEngine = mmapv1

version: '3.6'
services:
  mongo:
    image: rcmongo
    volumes:
      - "${PWD}/data:/data/db"
    entrypoint: mongod --smallfiles --storageEngine=mmapv1
  rocketchat:
    image: rc
    environment:
      ROOT_URL: 'http://localhost'
    ports:
      - "80:3000"
